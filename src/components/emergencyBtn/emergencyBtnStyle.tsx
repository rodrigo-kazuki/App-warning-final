import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
  emergencyBtn: {
    fontSize:30,
    backgroundColor:'red',
    borderColor:'black',
    borderWidth:2,
    borderRadius:5,
    padding:10
  
  }
});